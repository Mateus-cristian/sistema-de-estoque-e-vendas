<div class="flex flex-col gap-4 md:container md:mx-auto py-4">
  <h1 class="font-bold text-red-700 text-2xl">Bem-vindo ao sistema Estoque!</h1>
  <h2 class="text-lg text-neutral-600">Olá, <%= current_user.name %>!</h2>
  <div class="flex flex-col sm:flex-row gap-3 mt-2 w-full max-w-md">
    <%= link_to "Produtos", products_path, class: "button-solid" %>
    <%= link_to "Vendas", sales_path, class: "button-outline" %>
  </div>
</div>
